# 新建数据库
CREATE DATABASE mybatis
  DEFAULT CHARACTER SET utf8;

# 选择数据库
USE mybatis;

# 表名起自己的名字
CREATE TABLE Yangming
(
  id   INT PRIMARY KEY,
  col1 VARCHAR(20),
  col2 VARCHAR(20)
);


CREATE TABLE liuyachao
(
  id   INT PRIMARY KEY,
  col1 VARCHAR(20),
  col2 VARCHAR(20)
);

